# ============================================
# Configuration Backend MyBudget+
# ============================================
# Copiez ce fichier vers .env et remplissez les valeurs
# copy env.template .env

# ============================================
# Configuration Serveur
# ============================================
PORT=3001
NODE_ENV=development

# ============================================
# Base de données MongoDB (OBLIGATOIRE)
# ============================================
# Format: mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority
# Ou pour local: mongodb://localhost:27017/mybudget
MONGO_URI=mongodb://localhost:27017/mybudget

# ============================================
# Secrets de sécurité (OBLIGATOIRE)
# ============================================
# Générez ces secrets avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=votre_secret_jwt_très_long_et_aléatoire_ici
SESSION_SECRET=votre_secret_session_très_long_et_aléatoire_ici

# ============================================
# URL Frontend (OBLIGATOIRE)
# ============================================
# URL de votre frontend (local ou déployé)
FRONTEND_URL=http://localhost:5173

# ============================================
# Configuration Email (OPTIONNEL)
# ============================================
# Pour l'envoi d'emails (reset password, notifications)
# Gmail: Utilisez un App Password (voir GUIDE_EMAIL_RAPIDE.md)
EMAIL_USER=votre-email@gmail.com
EMAIL_PASS=votre-app-password-gmail

# ============================================
# OAuth Google (OPTIONNEL)
# ============================================
# Pour la connexion avec Google
# Créez un projet sur: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=votre-google-client-id
GOOGLE_CLIENT_SECRET=votre-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback

# ============================================
# reCAPTCHA (OPTIONNEL)
# ============================================
# Pour la protection anti-spam
# Créez une clé sur: https://www.google.com/recaptcha/admin
RECAPTCHA_SECRET_KEY=votre-recaptcha-secret-key

# ============================================
# PayPal (OPTIONNEL)
# ============================================
# Pour l'intégration PayPal
# Créez une app sur: https://developer.paypal.com/
PAYPAL_CLIENT_ID=votre-paypal-client-id
PAYPAL_CLIENT_SECRET=votre-paypal-client-secret
PAYPAL_MODE=sandbox
# PAYPAL_MODE=live (pour la production)

# ============================================
# Admin (OPTIONNEL)
# ============================================
# Code secret pour créer un compte admin
# Utilisé lors de l'inscription avec ce code
ADMIN_CREATION_CODE=MYBUDGET-ADMIN-2025

# ============================================
# Backend URL (OPTIONNEL)
# ============================================
# URL complète du backend (pour les callbacks PayPal, etc.)
# En local: http://localhost:3001
# Sur Render: https://votre-backend.onrender.com
# Sur Vercel: https://votre-backend.vercel.app
BACKEND_URL=http://localhost:3001

