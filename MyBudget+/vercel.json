{
  "buildCommand": "cd apps/web && npm install && vite build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "cd apps/web && npm install",
  "framework": "vite",
  "rootDirectory": "apps/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
